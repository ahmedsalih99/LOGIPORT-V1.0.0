# ============================================================
# LOGIPORT — Python Dependencies
# ============================================================
# التثبيت:
#   pip install -r requirements.txt
# ============================================================

# ── واجهة المستخدم ──────────────────────────────────────────
PySide6>=6.6.0

# ── قاعدة البيانات ──────────────────────────────────────────
SQLAlchemy>=2.0.0
# ملاحظة: Alembic لا يُستخدم — التهجير يتم عبر database/bootstrap.py

# ── كلمات المرور ────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# ── ملفات Excel ─────────────────────────────────────────────
openpyxl>=3.1.0

# ── قوالب HTML للمستندات ────────────────────────────────────
Jinja2>=3.1.0

# ── Playwright (محرك PDF الرئيسي) ───────────────────────────
# بعد التثبيت شغّل: playwright install chromium
playwright>=1.40.0

# ── WeasyPrint (محرك PDF احتياطي — اختياري) ─────────────────
# غير مضمَّن في EXE (مستثنى من main.spec) — للتطوير فقط
# Linux/macOS: sudo apt install libcairo2 libpango-1.0-0 libpangocairo-1.0-0
# Windows: GTK3 من https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer
# weasyprint>=62.0

# ── مكتبة مقارنة الإصدارات (نظام التحديثات) ────────────────
packaging>=23.0